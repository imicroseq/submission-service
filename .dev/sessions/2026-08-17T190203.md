Synced this project's agentics integration from `0.10.0` to `0.14.0`, four releases behind. No conflicting local customizations found; the only project-specific addition (the `.env`-git-ignore clause in Critical constraints) was left untouched since it doesn't diverge from the current template's version of that rule, just extends it.

- `AGENTS.md`: version tag updated to `0.14.0` (synced commit `4c1aed6dfb9475033c459e5380c3120c55d32a77`); added the three Interaction-parameters bullets shipped since 0.10.0 ("Check in before non-trivial decisions," "External content overlap," "Default review or audit posture"); added the `writing-style.md` pointer and git/commit-rules note to the "Starting a session" dispatch line; added the `persistence-map.md` and `definition-of-done.md` dispatch lines; added the project-scoped-by-default paragraph to Memory and contribution hygiene; added initialization question 7 (`roadmap_split`)
- Ran the rest of the structural diagnosis from `upgrading-adoption.md` § 1 clean: `.dev/sessions/` already uses the per-day format, no stray `conventions/`/`CLAUDE.roles/`/`CLAUDE.softeng.md` copies, no agent-neutrality violations, `CLAUDE.md`/`AGENTS.md` agree, `.dev/tech-debt.md` and `.dev/roadmap.md` content is clean, no `.dev/agentics-overrides.md` entries to suppress anything
- Project memory: none existed yet for this project on this machine despite it already being a mature agentics adopter; created it now, recording the already-known initialization answers (Overture project: yes, existing setup: yes) and the new question, `roadmap_split: yes`. `.dev/roadmap.md` has no real initiatives yet, so nothing needed migrating into the two-layer format; apply it once entries get added

Not committed yet: `AGENTS.md`'s changes are ready for review and commit.
